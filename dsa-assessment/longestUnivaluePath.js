/**
 * 
 */


const { BinaryTree } = require("../dsa-trees/binary-tree.js");

function longestUnivaluePath(bTree) {

  if (!(bTree instanceof BinaryTree)) throw new Error("Please submit a binary tree.")

}

module.exports = longestUnivaluePath;